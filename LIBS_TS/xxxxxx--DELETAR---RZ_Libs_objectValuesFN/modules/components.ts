/**
 * @returns disponibiliza os metodos : [_Log -> para consoleLog] + [ _sleep -> input demorado com time]
 * @default nomeado com _anderline, replique renomeando sem o _ -> e use a replica.
 * @exemple {_foo as foo} :: foo()
 */

export * from "./logConsole/features/feature.ts";
export * from "./inputSleep/features/feature.ts";
